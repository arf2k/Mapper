
const INITIAL_STATE = {
     map: null
}

const mapReducer = (state = INITIAL_STATE, action) => {
     switch(action.type) {

     
     default:
          return state 
     }
}

export default mapReducer
